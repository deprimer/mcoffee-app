platform :ios, '14.0'

# Specify the Xcode project path
project 'MCoffee.xcodeproj'

# Suppress the warning about the master specs repo
install! 'cocoapods', :warn_for_unused_master_specs_repo => false

target 'MCoffee' do
  # Add any pure Swift/iOS dependencies here if needed later
end
